{
  "name": "netspeed-metrics",
  "version": "1.0.0",
  "main": "dist/index.js",
  "repository": "git@github.priv:bruno-domazet/netspeed-metrics.git",
  "author": "bruno <bruno.domazet@pm.me>",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build --pretty",
    "clean": "rm -rf tsconfig.build",
    "start": "node dist/index.js",
    "fresh": "yarn clean && yarn build && yarn start"
  },
  "dependencies": {
    "@types/node": "^16.11.0",
    "fastify": "^3.22.0",
    "prom-client": "^14.0.0",
    "typescript": "^4.4.4"
  }
}
